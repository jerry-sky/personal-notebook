#!/bin/bash

# Git
alias ga="git add"
alias gaa="git add *"
alias gts="git status"
alias gcm="git commit -m"
alias gph="git push"
alias gpl="git pull"
alias gplr="git pull --rebase"
alias gf="git fetch"

# VS Code shortcuts
alias cx="code && exit"
alias ccx="code . && exit"
alias c="code"
alias ccc="code ."


